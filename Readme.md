# Plugin 组成部分
# # a. spider twisted.internet.selectreactor.SelectReactor / twisted.internet.asyncioreactor.AsyncioSelectorReactor
# whoami ---> determin user / sudo su -
# crontab format minute hour dom mon dow / crontab -e command

psql -U postgres -c "SHOW hba_file;"
psql -U postgres -c "SHOW data_directory;"

find /Library -name "pg_hba.conf" 2>/dev/null

host    bt_feed     postgres     192.168.2.100/32     md5 # pg

# benchmark minute tick parquet

# asset / adjustment / rightment /

export PYTHONDONTWRITEBYTECODE=1
